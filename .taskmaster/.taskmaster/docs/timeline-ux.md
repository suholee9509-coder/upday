# Timeline UX 기획서 v1.0
# Last Updated: 2026-02-04

---

## Overview

타임라인 기반 뉴스 플랫폼의 핵심 차별점인 **"시간이 유일한 축"**을 사용자가 체감할 수 있도록 하는 UX 기능 정의.

**핵심 기능:**
1. NOW 인디케이터 - 실시간성 체감
2. 날짜 드롭다운 셀렉트 - 시간 탐색 + 현재 위치 인지

---

## 1. NOW 인디케이터

### 1.1 목적

1시간 이내 발행된 뉴스를 시각적으로 강조하여 **"지금 일어나고 있는 일"**을 즉시 인지하게 함.

### 1.2 조건

| 조건 | 표시 |
|------|------|
| 발행 후 1시간 미만 | NOW 인디케이터 표시 |
| 발행 후 1시간 이상 | 표시 안 함 |

### 1.3 시각적 표현

**NewsCard 내 위치:** 시간 텍스트 앞

```
┌─────────────────────────────────────────────────────────────┐
│ OpenAI announces GPT-5 with reasoning capabilities          │
│ New model shows 2x improvement in complex reasoning tasks.  │
│ 🔴 12m ago  [AI]  TechCrunch ↗                              │
│ ↑                                                           │
│ NOW 인디케이터                                               │
└─────────────────────────────────────────────────────────────┘
```

**디자인 스펙:**

| 요소 | 스펙 |
|------|------|
| 아이콘 | 빨간 원 (●) 또는 pulse 애니메이션 dot |
| 색상 | `text-red-500` / `bg-red-500` |
| 크기 | 8px (w-2 h-2) |
| 애니메이션 | `animate-pulse` (선택적) |
| 간격 | 시간 텍스트와 4px (gap-1) |

**구현 예시:**

```tsx
function TimeDisplay({ publishedAt }: { publishedAt: Date }) {
  const isNow = Date.now() - publishedAt.getTime() < 60 * 60 * 1000 // 1시간
  const timeAgo = formatTimeAgo(publishedAt)

  return (
    <span className="flex items-center gap-1 text-xs text-muted-foreground">
      {isNow && (
        <span className="w-2 h-2 bg-red-500 rounded-full animate-pulse" />
      )}
      {timeAgo}
    </span>
  )
}
```

### 1.4 사용자 효과

| 상황 | 사용자 인식 |
|------|-------------|
| NOW 뉴스 3개 표시 | "지금 뭔가 터지고 있네" |
| NOW 뉴스 0개 | "지금은 조용하구나" |
| 아침에 앱 열었을 때 | "밤새 뭐가 있었는지" 빠른 파악 |

---

## 2. 날짜 드롭다운 셀렉트

### 2.1 목적

1. **현재 위치 인지**: 스크롤 중 "지금 며칠 치 뉴스를 보고 있는지" 표시
2. **시간 탐색**: 특정 날짜로 바로 점프

### 2.2 위치

FilterBar(chip 섹션) **가장 오른쪽**, 그리드 정렬

```
┌─────────────────────────────────────────────────────────────────────────┐
│ [All] [AI] [Startups] [Dev] [Product] [Research]          [Today ▼]    │
│ ←─────────────── 카테고리 필터 ───────────────→           ←─ 날짜 ─→   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.3 컴포넌트 구조

**기본 상태 (닫힘):**

```
┌──────────────┐
│ Today     ▼  │
└──────────────┘
```

**열린 상태:**

```
┌──────────────┐
│ Today     ▲  │
├──────────────┤
│ ● Today      │  ← 현재 선택됨
│ ○ Yesterday  │
│ ○ Feb 2      │
│ ○ Feb 1      │
│ ○ Jan 31     │
│ ─────────────│
│ ○ Earlier... │  ← 더 이전 날짜 (선택적)
└──────────────┘
```

### 2.4 날짜 표시 포맷

| 날짜 | 표시 |
|------|------|
| 오늘 | "Today" |
| 어제 | "Yesterday" |
| 올해 내 | "Feb 2", "Jan 31" (M월 D일) |
| 작년 이전 | "Dec 31, 2025" |

### 2.5 동작 정의

#### A. 스크롤 연동 (자동 업데이트)

```
사용자가 스크롤 →
현재 화면에 보이는 첫 번째 NewsCard의 날짜 감지 →
드롭다운 버튼 텍스트 자동 변경

예:
- 오늘 뉴스 영역 스크롤 중 → "Today"
- 어제 뉴스 영역 진입 → "Yesterday"
- 2/2 뉴스 영역 진입 → "Feb 2"
```

**구현 로직:**

```typescript
// Intersection Observer로 날짜 구분선 감지
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const date = entry.target.dataset.date
      setCurrentDate(date)
    }
  })
}, { threshold: 0, rootMargin: '-100px 0px 0px 0px' })
```

#### B. 날짜 선택 (수동 점프)

```
드롭다운 열기 →
날짜 선택 (예: "Yesterday") →
해당 날짜의 첫 번째 뉴스로 smooth scroll →
드롭다운 닫힘 →
버튼 텍스트 "Yesterday"로 업데이트
```

### 2.6 드롭다운 옵션 목록

**기본 옵션 (최근 7일):**

```
Today
Yesterday
{3일 전 날짜}
{4일 전 날짜}
{5일 전 날짜}
{6일 전 날짜}
{7일 전 날짜}
```

**확장 옵션 (선택적):**

```
───────────
This Week    → 이번 주 월요일로 점프
Last Week    → 지난 주 월요일로 점프
Pick Date... → 캘린더 피커 (향후)
```

### 2.7 디자인 스펙

**버튼 스타일:**

| 속성 | 값 |
|------|-----|
| 높이 | FilterChip과 동일 (32px, h-8) |
| 패딩 | px-3 |
| 배경 | bg-secondary (기본), bg-accent (열림) |
| 테두리 | border-border |
| 라운드 | rounded-md |
| 폰트 | text-sm font-medium |
| 아이콘 | ChevronDown (▼), 열림 시 ChevronUp (▲) |

**드롭다운 패널:**

| 속성 | 값 |
|------|-----|
| 너비 | 버튼과 동일 또는 min-width 120px |
| 배경 | bg-popover |
| 테두리 | border-border |
| 그림자 | shadow-md |
| 라운드 | rounded-md |
| 최대 높이 | max-h-64 (스크롤) |

**옵션 아이템:**

| 속성 | 값 |
|------|-----|
| 높이 | 36px |
| 패딩 | px-3 |
| 호버 | bg-accent |
| 선택됨 | ● 표시 + font-medium |

### 2.8 반응형 처리

**Desktop (lg+):**
- FilterBar 오른쪽에 고정 표시
- 카테고리 필터와 같은 행

**Tablet (md):**
- 동일

**Mobile (<md):**
- FilterBar가 가로 스크롤이므로, 날짜 드롭다운은 **스크롤 영역 밖** 오른쪽 고정

```
Mobile:
┌─────────────────────────────────────────────────┐
│ ←[All][AI][Startups][Dev]...→        [Today ▼] │
│   ←── 스크롤 영역 ──→                 ←고정→   │
└─────────────────────────────────────────────────┘
```

### 2.9 URL 연동 (선택적)

날짜 선택 시 URL에 반영하여 공유/북마크 가능하게 할 수 있음.

```
/timeline?date=2026-02-03  → 해당 날짜로 점프
/timeline                  → 오늘 (기본)
```

---

## 3. FilterBar 레이아웃 수정

### 3.1 현재 구조

```
[All] [AI] [Startups] [Dev] [Product] [Research]
```

### 3.2 수정 후 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ┌───────────────────────────────────────────────┐  ┌──────────────┐    │
│ │ [All] [AI] [Startups] [Dev] [Product] [Research] │  │ [Today ▼]    │    │
│ │ ←───────────── flex-1, gap-2 ───────────────→ │  │ ← shrink-0 → │    │
│ └───────────────────────────────────────────────┘  └──────────────┘    │
│ ←─────────────────── justify-between ──────────────────────────────→   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.3 구현 예시

```tsx
function FilterBar() {
  return (
    <div className="flex items-center justify-between gap-4">
      {/* 카테고리 필터 */}
      <div className="flex items-center gap-2 overflow-x-auto scrollbar-hide">
        <FilterChip value="all" label="All" />
        <FilterChip value="ai" label="AI" />
        <FilterChip value="startups" label="Startups" />
        <FilterChip value="dev" label="Dev" />
        <FilterChip value="product" label="Product" />
        <FilterChip value="research" label="Research" />
      </div>

      {/* 날짜 드롭다운 */}
      <DateDropdown />
    </div>
  )
}
```

---

## 4. 구현 체크리스트

### Phase 1: NOW 인디케이터

- [ ] `formatTimeAgo` 유틸리티에 1시간 미만 체크 로직 추가
- [ ] NewsCard 컴포넌트에 NOW dot 추가
- [ ] pulse 애니메이션 적용

### Phase 2: 날짜 드롭다운

- [ ] DateDropdown 컴포넌트 생성
- [ ] 드롭다운 옵션 목록 (최근 7일)
- [ ] 스크롤 연동 (Intersection Observer)
- [ ] 날짜 선택 시 스크롤 점프
- [ ] FilterBar에 DateDropdown 배치
- [ ] 모바일 반응형 처리

### Phase 3: 개선 (선택적)

- [ ] URL 연동 (?date=)
- [ ] "Earlier..." 또는 캘린더 피커
- [ ] 날짜별 뉴스 카운트 표시

---

## Document Metadata

- **Version**: 1.0
- **Created**: 2026-02-04
- **Status**: Active
- **Related Docs**:
  - IA: `.taskmaster/docs/IA.txt`
  - PRD: `.taskmaster/docs/prd.txt`
  - Design System: `.taskmaster/docs/design-system.md`
  - Screen Inventory: `.taskmaster/docs/screen-inventory.md`
